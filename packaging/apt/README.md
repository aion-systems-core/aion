# APT packaging scaffold

This directory is a scaffold for Debian packaging metadata (`debian/` control files).

Recommended next steps:

1. Add `debian/control`, `debian/rules`, and changelog.
2. Build package with `dpkg-buildpackage`.
