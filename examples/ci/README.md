# CI-oriented examples

For capsule governance baselines and checks, prefer:

- `examples/governance/ci_baseline.sh`
- `examples/governance/ci_check.sh`

For command-run baselines (RunResult JSON), see `aion control ci` in the CLI help.
